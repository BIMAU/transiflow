name: Run tests

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: 'ubuntu-latest'
            dependencies: ''
            numproc: 1
          - os: 'macos-latest'
            dependencies: 'nlesc/nlesc/trilinos'
            numproc: 1
          - os: 'macos-latest'
            dependencies: 'nlesc/nlesc/trilinos'
            numproc: 4

    runs-on: ${{ matrix.os }}

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.9
      uses: actions/setup-python@v2
      with:
        python-version: 3.9

    - name: Install python dependencies
      run: |
        python -m pip install --upgrade pip
        pip install flake8 pytest
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

    - name: Install Linux dependencies
      run: |
        sudo apt-get install ${{ matrix.dependencies }}
      if: ${{ matrix.os != 'macos-latest' && matrix.dependencies != '' }}

    - name: Install macOS dependencies
      run: |
        brew tap nlesc/nlesc
        brew install ${{ matrix.dependencies }}
      if: ${{ matrix.os == 'macos-latest' && matrix.dependencies != '' }}

    - name: Compiler cache
      uses: hendrikmuhs/ccache-action@v1
      with:
        key: fvm-${{ matrix.os }}
        max-size: 500M

    - name: Add ccache to the path
      run: |
        echo "/usr/lib/ccache" >> $GITHUB_PATH
        echo "/usr/local/opt/ccache/libexec" >> $GITHUB_PATH
        
    - name: Set env.HOME
      run: echo ::set-env name=HOME::$HOME
      shell: bash

    - name: Lint with flake8
      run: |
        flake8 . --count --show-source --statistics

    - name: Clone JaDaPy
      uses: actions/checkout@v2
      with:
        repository: BIMAU/jadapy
        path: jadapy

    - name: Install JaDaPy
      run: |
        cd jadapy
        pip install .

    - name: Clone HYMLS
      uses: actions/checkout@v2
      with:
        repository: nlesc-smcm/hymls
        path: hymls
      if: ${{ matrix.os == 'macos-latest'}}

    - name: Build HYMLS
      run: |
        cd hymls
        mkdir build
        cd build
        cmake -DCMAKE_INSTALL_PREFIX=${{ env.HOME }} ..
        make -j
        make install
      if: ${{ matrix.os == 'macos-latest'}}

    - name: Test with pytest
      run: |
        pytest -s tests
      if: ${{ matrix.numproc == 1 }}

    - name: Test with parallel pytest
      run: |
        mpiexec -np ${{ matrix.numproc }} --oversubscribe python tests/test_HYMLS.py
      env:
        PYTHONPATH: ${{ env.HOME }}/lib/python3.9/site-packages:/usr/local/opt/trilinos/lib/python3.9/site-packages:.
      if: ${{ matrix.numproc != 1 }}
